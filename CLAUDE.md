# Claude Code 项目指南

## 项目概述
这是一个面向中文小白用户的Claude Code入门教程网站，手把手带领完全没有编程基础的用户学会安装和使用Claude Code。

## 项目结构
```
学习Claude code/
├── index.html          # 首页诊断问卷
├── learn.html          # 教程学习页面
├── css/
│   └── style.css       # 全局样式
├── js/
│   ├── app.js          # 主应用逻辑
│   ├── steps.js        # 引导式步骤组件
│   ├── platform.js     # 平台切换器
│   ├── quiz.js         # 诊断问卷逻辑
│   └── copy.js         # 代码复制功能
└── test-*.html         # 各种测试页面
```

## 自动停止与求助规则

- 修复同一问题超过 **2次尝试** 仍失败 → 立即停止，汇报卡点，等待指示
- 单次任务连续工具调用超过 **20次** → 暂停，汇报进度，确认是否继续
- 修改文件前必须先说明假设，修改后必须用 Playwright 或测试验证
- 禁止"猜测性修改"：没有明确根因就不动代码

## 开发进度

### 已完成的阶段
- ✅ **第一阶段**：项目初始化 - 基础HTML结构和样式
- ✅ **第二阶段A**：引导式步骤组件 - 分步骤教程界面
- ✅ **第二阶段B**：左侧目录侧边栏 - 固定导航菜单
- ✅ **第二阶段C**：平台切换器和代码复制按钮 - Mac/Windows切换，代码复制功能
- ✅ **第三阶段**：首页诊断问卷 - 引导式问答系统

### 技术栈
- 纯原生JavaScript（无框架依赖）
- HTML5 + CSS3
- localStorage用于本地数据持久化

### 关键功能
1. **诊断问卷**：5道选择题，引导式回答，进度显示
2. **平台切换**：Mac/Windows标签页切换，本地存储记住用户选择
3. **代码复制**：一键复制代码块内容，带视觉反馈
4. **左侧导航**：固定侧边栏，支持模块间导航
5. **引导式步骤**：分步骤教程界面，清晰的学习路径
